<table>
  <tbody>
      <% spot.reviews.each do |review| %>
      <tr>
        
        <td>
          <%= link_to admin_customer_path(review.user) do %>
            <%= image_tag review.customer.get_profile_image, size: "100x100" %><br>
            <%= review.customer.name %>
          <% end %>
        </td>
        <td><%= review.comment %></td>
        <!--<td>-->
        <!--  <%# if review.user == current_user %>-->
        <!--    <%#= link_to "Destroy", book_book_comment_path(book, book_comment), method: :delete, class: "btn btn-danger pull-right", remote: true %>-->
        <!--  <%# end %>-->
        <!--</td>-->
      </tr>
      <% end %>
  </tbody>
</table>