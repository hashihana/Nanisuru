<div class="container wrapper flex">
 <div class="row">
		<h2><%= @customer.nickname %>さんの注文履歴一覧</h2>
	<table class="table">
		<thead>
			<tr>
				<th>レビュー日</th>
				<th>スポット</th>
				<th>レビュー詳細</th>
			</tr>
		</thead>
		<tbody>
			<% @reviews.each do |review| %>
				<tr>
					<td><%= review.created_at.strftime("%Y/%m/%d %H:%M:%S") %></td>
					<!--<td>-->
					<!--	<%# if review.customer.present? %>-->
					<!--	  <%#= link_to review.customer.nickname, admin_customer_path(review.customer) %>-->
					<!--	<%# end %>-->
					<!--</td>-->
					<td><%= link_to review.@spot.name, admin_spot_path(@spot) %>
					<p><%= review.@spot.name %></p></td>
					<td><%= review.comment %></td>
				<!--	<td><%#= link_to "削除", spot_review_path(@spot, @review), method: :delete, class: "btn btn-danger pull-right", remote: true %></td>-->
				<!--</tr>-->
			<% end %>
		</tbody>
	</table>
	</div>
	</div>
	<%= paginate @reviews %>
